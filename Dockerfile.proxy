FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY kafka-websocket-proxy.mjs ./
COPY scripts/load-env.mjs ./scripts/load-env.mjs

EXPOSE 8080

CMD ["node", "kafka-websocket-proxy.mjs"]
